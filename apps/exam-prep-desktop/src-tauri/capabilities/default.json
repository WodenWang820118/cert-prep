{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for the exam prep app.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}
