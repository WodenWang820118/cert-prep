# Online-package E2E

This directory is intentionally empty until an explicitly pinned online
Cert Prep package and its runtime provenance are supplied. It is not allowed
to reuse the local-package worker/runtime or silently fall back to mocks.
