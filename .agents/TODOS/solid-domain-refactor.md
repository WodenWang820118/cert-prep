# SOLID Domain Refactor TODO

Status: completed. 本輪可執行項目已吸收到 `.agents/SPECS/solid-domain-refactor.md` 作為後續 QA 依據。

## 後續仍屬其他 backlog

- OCR health settle / stale drawer 狀態：保留在 parallel parsing / runtime UX 後續。
- First chunk latency 與 worker count 效能：保留在 `.agents/TODOS/parallel-parsing-reasoning.md`。
- Packaged app graceful close / process-tree cleanup hardening：保留在 `.agents/TODOS/parallel-parsing-reasoning.md`。
- Reasoning model live bakeoff：保留在 `.agents/TODOS/parallel-parsing-reasoning.md`。
- Angular bundle budget 與 i18n/copy cleanup：另行規劃，不混入本輪 pure refactor。
