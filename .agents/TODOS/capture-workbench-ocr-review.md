# Capture Workbench review handoff

- [ ] Run the real published v0.4.1 engine-bearing PDF/image/audio smoke on the
  target Windows x64 machine after explicit consent and engine installation;
  verify the typed pull-session output and reviewed overlays preserve raw
  provenance through consumer-local mapping, runtime commit, and durable
  storage. Do not close this with a source-import/registry prototype or
  deterministic fixture.
